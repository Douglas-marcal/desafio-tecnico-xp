import { RegisteredClient } from '../../../../interface';

const registeredClient: RegisteredClient = {
  CodCliente: 1,
  email: 'test@test.com',
  message: 'Cliente registrado.',
  Saldo: 0,
};

export default registeredClient;
